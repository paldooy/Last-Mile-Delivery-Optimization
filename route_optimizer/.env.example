# Route Optimizer Environment Variables

# OSRM Server URL (leave as localhost:5000 or use public demo server)
OSRM_URL=http://localhost:5000
# Alternative public demo: OSRM_URL=http://router.project-osrm.org

# Server port
PORT=8000

# Cache directory for geocoding and distance matrix
ROUTE_CACHE_DIR=./.cache

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Nominatim user agent (replace with your email)
USER_AGENT=RouteOptimizer/1.0 (your-email@example.com)
